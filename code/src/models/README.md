# Model Layer

This folder contains Java Beans or POJOs representing the application’s data structure. These models are used to store and transfer data between layers.

### Example Responsibilities
- Representing a trade record with fields like `date`, `symbol`, `price`, `quantity`.
- Providing getter and setter methods for encapsulated data.

Follow naming conventions and ensure models match the database schema.